name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  # Cancel in-flight runs for a PR when a new commit lands. `main` pushes
  # are never cancelled so the post-merge build always completes.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv pip install --system ruff
      - run: ruff check src tests
      - run: ruff format --check src tests

  typecheck:
    name: Typecheck (mypy --strict)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv pip install --system -e ".[test,dev]"
      - run: mypy --strict src/hermes_plugin_sync

  test:
    name: Test (py${{ matrix.python }} / ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        include:
          # Bookend macOS coverage: oldest + newest supported Python. Full
          # matrix on macOS roughly triples CI minutes for minimal marginal
          # signal — bugs tend to be OS-specific or Python-version-specific
          # independently.
          - os: macos-latest
            python: "3.10"
          - os: macos-latest
            python: "3.14"
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python }}
      - run: uv pip install --system -e ".[test]"
      - run: pytest -v

  coverage:
    name: Coverage (gate at 80%)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv pip install --system -e ".[test]"
      - run: pytest --cov=hermes_plugin_sync --cov-report=term-missing --cov-fail-under=80
